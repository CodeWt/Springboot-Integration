职责链模式
```aidl
使多个对象都有机会处理请求，从而避免请求的发送者和接收者之前的耦合关系。将这个对象连成一条链，并沿着这条链传递该请求，直到有一个对象处理它为止。
```
职责链的好处
```aidl
1.当提交一个请求的时候，请求是沿着链传递直至有一个ConcreteHandler 对象负责处理它。
2.接收者和发送者都没有对方的明确的信息，且链中的对象自己也并不知道链的结构。它们仅需要一个指向后继者的引用，而不用持有所有后继者的引用，大大降低了耦合度。
3.可以随时增加或修改处理一个请求的结构链。
```